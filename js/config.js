// Single place to point the frontend at your deployed backend.
// GitHub Pages serves static files only, so this must be an absolute URL
// to your Render (or other) backend deployment.
window.CTF_CONFIG = {
  API_BASE_URL: "https://your-backend.onrender.com",
};
